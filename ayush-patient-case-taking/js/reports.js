/* ==========================================================================
   AYUSH CARE — Reports
   Generate/Print/Export are UI placeholders until the backend exists.
   ========================================================================== */

document.addEventListener("DOMContentLoaded", function () {
  document.querySelectorAll(".report-card button").forEach(function (btn) {
    btn.addEventListener("click", function () {
      alert("This is a UI preview — report generation will be enabled once the backend and database are built.");
    });
  });
});
